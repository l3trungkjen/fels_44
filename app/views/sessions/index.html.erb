<%= form_for :session, :html => { class: 'form-signin' }, url: sessions_path do |f| %>
  <h2>Sign in</h2>
  <% if flash[:error] %>
    <p class="bg-danger"><%= flash[:error] %></p>
  <% end %>
  <%= f.text_field :email, class: 'form-control', placeholder: 'Input email...' %>
  <%= f.password_field :password, class: 'form-control', placeholder: 'Input password...' %>
  <%= f.submit 'Sign in', class: 'btn btn-lg btn-primary btn-block' %>
  <p>Create new user? <%= link_to 'Sign up now!', new_user_path %></p>
<% end %>